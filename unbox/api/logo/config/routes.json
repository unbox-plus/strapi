{
  "routes": [
    {
      "method": "GET",
      "path": "/logos",
      "handler": "logo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/logos/count",
      "handler": "logo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/logos/:id",
      "handler": "logo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/logos",
      "handler": "logo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/logos/:id",
      "handler": "logo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/logos/:id",
      "handler": "logo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
